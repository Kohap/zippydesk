# Meta Cloud API
META_ACCESS_TOKEN=
META_PHONE_NUMBER_ID=
META_WEBHOOK_VERIFY_TOKEN=
META_WEBHOOK_PORT=3000

# Gemini Vision (receipt extraction)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Stub vision (dev only — returns this receipt JSON for every image)
VISION_RECEIPT_BODY=

# Postgres (optional — falls back to in-memory repos when empty)
DATABASE_URL=

# Static vendor config JSON (optional — falls back to bundled demo vendor)
VENDOR_CONFIG_PATH=