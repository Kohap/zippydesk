# - Prefers comprehensive, structured prompts that explicitly define role, design system, page structure, and technical standards upfront. Confidence: 0.85
- Prefers comprehensive, structured prompts that explicitly define role, design system, page structure, and technical standards upfront. Confidence: 0.85
- Aesthetic reference points for SaaS marketing UI: Stripe, Vercel, Linear (modern, minimalist, tech-forward). Confidence: 0.9
- Brand uses a slate/dark base with a blue-to-teal gradient (`#006b8c` → `#00bca3`) for primary actions and active states. Confidence: 0.85
- Targets the Nigerian market — pricing in Naira (₦), payment via Paystack. Confidence: 0.9
- Tech stack: Next.js 15 (App Router with route groups like `(app)`), Prisma, Tailwind v4. Confidence: 0.95
- Project layout convention: `app/(group)/{route}/page.tsx`, `components/{landing,ui,brand,app}`, `lib/{api,utils,context,hooks}`, `prisma/schema.prisma`, `types/`. Confidence: 0.9
- Uses a custom design-token CSS variable system rather than raw hex values — observed classes like `bg-ink`, `text-ink-text`, `text-ink-muted`, `text-aqua-bright`, `border-line`, plus an `assist-focus` utility for focus rings. Confidence: 0.85
- Requires strict TypeScript typing and robust handling for all data structures (state machines, orders, wallets, inventory). Confidence: 0.9
- Mobile-first responsiveness is mandatory — merchants manage stores from phones. Confidence: 0.85
- Likes to verify work with `npx tsc --noEmit`, `npx next build`, and `npm test` before declaring a task complete. Confidence: 0.85
- Test suite uses vitest; values keeping tests green (75 tests across 12 files maintained). Confidence: 0.8
- Prefers modular component architecture: separate `ui/` primitives (Badge, Button, Card, Dialog, Input, Switch), `brand/` (Logo), `app/` (feature views), `landing/` (marketing). Confidence: 0.9
- Likes polished, branded `not-found.tsx` pages rather than Next.js defaults — sees missing error/empty states as a deliverable gap. Confidence: 0.75
- Values "production-ready" framing — clean build, passing tests, no stubs or placeholder pages. Confidence: 0.9
- Treats polish as iterative over multiple passes — explicit "perfect the frontend" directives are followed by full audit → todo list → execute → verify cycles, not single-shot edits. Confidence: 0.8
- Eliminates hardcoded duplicates by extracting shared data to `lib/*.ts` modules (e.g., pricing tiers used by both landing calculator and billing view must come from one source to prevent drift). Confidence: 0.85
- Prefers semantic spec-defined input controls over freeform numbers — lead-capture form uses a qualifying dropdown of buckets mapped to integer midpoints in the API, rather than asking users to guess. Confidence: 0.8
- Dislikes redundant sections and filler copy — explicitly removes sections that just rephrase the hero, replaces 01/02/03 step numbers with icon tiles, drops "love your work" tone. Confidence: 0.8
- Replaces hand-rolled custom toggles/spinners with the real UI primitive (e.g., a `<button role="switch">` becomes `<Switch>` from the design system). Confidence: 0.85
- Adds design tokens for every color variant before using them — never raw hex in components; soft tonal backgrounds (`-soft`), borders (`-line`), and text colors (`-text`) are CSS variables mapped via Tailwind v4 `@theme`. Confidence: 0.9
- Builds accessible interactive widgets: FAQ accordions use `aria-expanded`/`aria-controls`/`hidden`, mobile menu has body-scroll lock + ESC-to-close + focus management, dialogs label their content with `DialogTitle`/`DialogDescription`. Confidence: 0.85
- Prefers verb-first control labels over noun labels — "Operational controls" replaces "Quick actions", "Top up credits" replaces a generic link. Confidence: 0.7
- Calls out mobile UX as a deliverable — mobile menu, touch-target sized hit areas (`min-h-[44px]`), and safe-area-inset padding are part of polish, not optional. Confidence: 0.85
- Provides both loading skeleton AND error boundary per route group; uses `app/global-error.tsx` for root-level failures (Next.js 15 quirk: root `error.tsx` breaks static manifest discovery). Confidence: 0.85
- Adds an "Add item" / creation affordance to inventory lists — toggle-only inventory is incomplete; merchant needs CRUD, not just state changes. Confidence: 0.75
- Uses icon-tile + short-copy cards over section-numbered steppers for feature explanations. Confidence: 0.75
- Credibilizes hero stats with real numbers (e.g., "Schema validation 99.2%") rather than marketing-fluff ("Concurrency infinite"). Confidence: 0.75
- Success/thank-you states for forms are explicit screens with numbered next steps, not a generic toast. Confidence: 0.75
- Values live motion and imagery on the marketing surface — wants the landing page to feel animated and alive (looping WhatsApp conversation, drifting ambient glow, staggered bubble entrances, pulse-ring live indicators, floating hero mockups) rather than static screenshots. Confidence: 0.85
- All animations must be gated behind a `prefers-reduced-motion: reduce` media query that disables `animation` and `transform` — motion is decorative, never required. Confidence: 0.85
- References and uses external style/quality audit checklists (e.g., pols.dev/slop) as an explicit correctness rubric — expects the model to fetch the rule list and audit recent edits against it, then fix violations. Confidence: 0.75
